node_modules/
*.log
.DS_Store

# Don't ignore packages/seiro/dist - it's built output that's needed
# But do ignore other dist folders
/example/dist/
/template/dist/

# Model CLI artifacts
model.db
.cache/

stuff
