/**
 * HTML Generation for CQRS Domain Model Viewer
 *
 * Based on modeling2 patterns - clean index with proper navigation.
 */
export declare function generateIndexHtml(actors: string[], documents: string[], sequences: string[], entities: string[], commands: {
    name: string;
    documents: string[];
}[]): string;
//# sourceMappingURL=html.d.ts.map